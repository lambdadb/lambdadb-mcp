# npm distribution and live validation — 2026-09-20

Dates in the release guides use KST. The installed live run finished at
`2026-09-19T16:22:45.935Z` (UTC). This is evidence for a specific candidate,
not a promise about future releases or every MCP client.

## Published candidate

- Package: `@functional-systems/lambdadb-mcp@0.1.0-dev.4`.
- Source/gitHead: `b79690aeccf4cfc87756153511dd286ff9ff7da1`.
- [OIDC publication workflow](https://github.com/lambdadb/lambdadb-mcp/actions/runs/35434355648/attempts/2): all Node 22.14.0, 22 and 24 validation jobs and the publishing job succeeded.
- The workflow tested the same tarball it published. The registry tarball digest,
  provenance source commit, repository, and workflow matched.
- Clean registry installation passed 23 consumer stdio tests. `npm audit signatures`
  verified registry signatures and provenance during publication verification.
- Channel snapshot: `dev=0.1.0-dev.4`, `latest=0.1.0-dev.1` (bootstrap prerelease).
  No rc/stable or GitHub Release exists at this checkpoint.

Verified registry and consumer lockfile integrity:

```text
sha512-zpFczQsIq5wSDIFWEwrIbK0tOJf5bTNKtLrazZ8NIN/vF/qwsgIFQbKoKdRXd+7dXNHyRsKJQgLasFQoMFFpGg==
```

## Separate verification scopes

| Verification | Transport / backend | Result |
| --- | --- | --- |
| Source checks | Local contracts and loopback fixtures | 44 tests passed |
| Installed publication artifact | Actual stdio / loopback fixtures | 23 tests passed |
| Existing source live integration | In-memory MCP / designated development service | Passed; temporary collection absence verified |
| npm-installed `0.1.0-dev.4` live smoke | Actual stdio / designated development service | Passed; temporary collection absence verified |

The source live integration also observed actual HTTP 201/202 responses and
`docsUrl` downloads, checked download authentication-header isolation, pagination,
filtering, sorting, and Branch/Tag/Alias reads. It completed in approximately
93 seconds. It was run separately from installed-package validation.

## Installed-package live smoke

The user explicitly designated the configured project as a development project
and authorized live writes. A fresh consumer directory installed the exact npm
version with `--ignore-scripts`. A one-off verification harness launched
`node_modules/.bin/lambdadb-mcp` with that consumer directory as its working
directory. The server used only installed files and explicitly supplied connection
environment variables; it did not use a source checkout or a dotenv loader.

Runtime: v24.15.0. Duration: approximately 62 seconds.

The first server process omitted the write opt-in, reported the installed version,
listed five read-only tools, and successfully called the live collection-list tool.
It exited successfully on stdin EOF. A second process explicitly enabled writes
and listed eight tools. It created one uniquely named temporary collection,
wrote two 3 MiB documents, and returned their complete payloads through Query,
Fetch, and List over stdio. SDK calls set up Branch/Tag/Alias references and cleaned
up the collection because those management operations are not MCP tools.

Sanitized execution log:

```text
Installed bin initialize/version, read-only tools/list, live tools/call, protocol-only stdout and EOF: PASS
Explicit write opt-in, collection create/get: PASS
Consistent Fetch: PASS (0s)
Committed List: PASS (55s)
Query/Fetch/List full two-document 6 MiB payload over stdio: PASS
Branch write: PASS (0s)
Branch delete: PASS (0s)
Branch/Tag/Alias refs and branch write isolation: PASS
Temporary collection absence: PASS (0s)
Installed-package live validation: PASS
```

All stdout lines from both server processes parsed as MCP JSON-RPC; stderr was
empty and both processes exited with code 0 on EOF. The original credential file
was byte-for-byte unchanged. No credentials, service URLs, project identifiers,
signed download URLs, or document payloads are included in this record.

This smoke verifies full large responses, but it does not directly intercept HTTP
inside the installed child process to prove which offload path was used. The
separate source live integration supplies that HTTP-level evidence. Live checks
remain opt-in and are not enabled in CI.

## Remaining release work

The dev distribution path is operational. Before rc/stable, promote the publishing
workflow and reviewed matching version/changelog metadata to main, validate that
candidate, and explicitly publish the corresponding GitHub prerelease/release.
See [RELEASING.md](../../RELEASING.md). No new npm version, GitHub tag, or Release
was created by this documentation follow-up.
